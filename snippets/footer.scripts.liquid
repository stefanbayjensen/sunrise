{%- liquid

  case template.name
    when 'product'
      assign template_script = 'product.js'
    else
      assign template_script = nil    
  endcase

-%}

<script src="{{- 'index.js' | asset_url -}}" defer></script>
<script src="{{- 'base.js' | asset_url -}}" defer></script>

{%- if template_script -%}
  <script src="{{- template_script | asset_url -}}" defer></script>
{%- endif -%}