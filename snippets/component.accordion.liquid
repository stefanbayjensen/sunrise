<div class="accordion {% if active %}accordion--active{% endif %}" data-component="accordion" data-group="{{ group }}">
  <button type="button" class="accordion__header" data-toggle>
    <span class="accordion__title">{{- title -}}</span>
  </button>
  <article class="accordion__inner" data-inner>{{- content -}}</article>
</div>