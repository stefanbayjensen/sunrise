{%- liquid
  assign type = type | default: 'button'
  assign translated_text = text | t
  
  unless translated_text contains 'translation missing:'
    assign text = translated_text
  endunless
-%}

<button name="{{ name }}" class="focus:outline-none focus:ring ring-primary/60 bg-primary text-white hover:bg-primary/90 focus:bg-primary/90 rounded px-6 py-4 font-semibold disabled:bg-gray-300 disabled:text-gray-500 w-full md:w-auto transition-colors duration-300 {{ class }}" type="{{- type -}}" {{ attributes }}>
  {{- text -}}
</button>